# Python dependencies for VRX Scripts_git (exclude ROS 2 apt packages)
# - Do NOT pip install: rclpy, sensor_msgs, geometry_msgs, std_msgs, cv_bridge
# - Install ROS 2 (e.g., Humble) and cv_bridge via apt

# Core numerical + imaging
numpy==1.26.4
opencv-python==4.10.0.84
pillow==10.4.0

# PyTorch stack (CPU wheels by default; install CUDA-specific wheels if needed)
# For CUDA-enabled envs, follow https://pytorch.org/get-started/locally/
torch==2.2.2
torchvision==0.17.2

# Required by MiDaS DPT models (used via torch.hub)
timm==0.9.16

# ONNX runtime for inference
onnxruntime==1.18.0
# If you need GPU acceleration for ONNXRuntime, consider:
# onnxruntime-gpu==1.18.0

# OWL-ViT / NanoOWL dependencies
transformers==4.44.2
huggingface-hub==0.24.6
tokenizers==0.19.1
safetensors==0.4.5

# Convenience (often installed as transitive deps but pinned for stability)
requests==2.32.3
filelock==3.15.4
urllib3==2.2.2
regex==2024.9.11
tqdm==4.66.5
